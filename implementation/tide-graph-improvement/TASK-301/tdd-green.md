# TASK-301: 統合テストスイートの拡張 - 最小実装 (Green Phase)

## 🎯 実装方針

テストを通すための最小限の実装を行います。Red Phase で失敗したテストを段階的に修正していきます。

## 🔧 実装項目

### 1. 基本的なdata-testid属性の追加

既存コンポーネントに不足している `data-testid` 属性を追加します。

### 2. 統合テスト用のテストヘルパー作成

E2Eテストで使用するヘルパー関数とモック機能を実装します。

### 3. パフォーマンス表示機能の最小実装

TASK-202のパフォーマンス監視結果を画面に表示する最小機能を追加します。

### 4. アクセシビリティ属性の追加

ARIA属性とキーボードナビゲーション対応の最小実装を追加します。

---

## 📝 実装詳細

### Phase 1: 基本data-testid属性の追加