# アーキテクチャ設計書

**Bite Note** - 釣果記録PWAアプリ

**バージョン**: v1.0
**最終更新**: 2025年10月30日

---

## 📋 技術スタック

### コア技術
- **React** 18.2.0 - UIライブラリ
- **TypeScript** 5.8.3 - 型安全な開発
- **Vite** 4.5.3 - 高速ビルドツール

### 状態管理
- **Zustand** 4.4.7 - 軽量状態管理

### データ永続化
- **Dexie.js** 3.2.4 - IndexedDBラッパー

### スタイリング
- **Tailwind CSS** 3.4.17 - ユーティリティファーストCSS

### 地図表示
- **Leaflet** 1.9.4 - 地図ライブラリ
- **React Leaflet** 4.2.1 - React統合

### グラフ表示
- **Recharts** 3.2.1 - データビジュアライゼーション

### フォーム管理
- **React Hook Form** 7.62.0 - フォームバリデーション
- **Zod** 3.25.76 - スキーマバリデーション

### テスト
- **Vitest** 1.0.4 - ユニットテスト
- **Playwright** 1.55.0 - E2Eテスト
- **Testing Library** 14.1.2 - Reactコンポーネントテスト

---

## 🏗️ アーキテクチャ概要

### レイヤー構成

Bite Noteは**4層アーキテクチャ**で構成されています。

```
┌─────────────────────────────────────────────────────┐
│         プレゼンテーション層（UI Layer）               │
│  React Components, Pages, Navigation                │
│  - components/ - UIコンポーネント（62個）             │
│  - pages/ - ページコンポーネント（将来分割予定）        │
└─────────────────────────────────────────────────────┘
                       ↓↑
┌─────────────────────────────────────────────────────┐
│      アプリケーション層（Application Layer）           │
│  State Management, Routing                          │
│  - stores/ - Zustand状態管理（2個）                   │
│  - hooks/ - カスタムフック（4個）                      │
└─────────────────────────────────────────────────────┘
                       ↓↑
┌─────────────────────────────────────────────────────┐
│     ビジネスロジック層（Business Logic Layer）         │
│  Services, Domain Logic                             │
│  - lib/ - サービス層（18個）                          │
│  - services/ - ドメインサービス（潮汐、魚種検索）       │
│  - utils/ - ユーティリティ（分析、スケール計算）        │
└─────────────────────────────────────────────────────┘
                       ↓↑
┌─────────────────────────────────────────────────────┐
│         データ層（Data Layer）                        │
│  Database, Persistence                              │
│  - lib/database.ts - IndexedDB（Dexie.js）          │
│  - data/ - マスターデータ（魚種231種）                 │
└─────────────────────────────────────────────────────┘
```

---

## 📂 ディレクトリ構造

```
bite-note/
├── src/
│   ├── components/          # UIコンポーネント（62個）
│   │   ├── ui/             # 基礎UIコンポーネント
│   │   │   ├── Button.tsx
│   │   │   ├── ModernCard.tsx
│   │   │   └── Skeleton.tsx
│   │   ├── layout/         # レイアウトコンポーネント
│   │   │   ├── AppLayout.tsx
│   │   │   ├── ModernHeader.tsx
│   │   │   └── ResponsiveGrid.tsx
│   │   ├── navigation/     # ナビゲーション
│   │   │   └── BottomNavigation.tsx
│   │   ├── chart/          # グラフコンポーネント
│   │   │   ├── TideChart.tsx
│   │   │   └── TrendChart.tsx
│   │   ├── map/            # 地図コンポーネント
│   │   │   └── FishingMap.tsx
│   │   ├── home/           # ホーム画面セクション
│   │   │   ├── RecentRecordsSection.tsx
│   │   │   ├── SpeciesChartSection.tsx
│   │   │   └── TideStatisticsSection.tsx
│   │   ├── FishingRecordForm.tsx    # 記録フォーム
│   │   ├── FishingRecordList.tsx    # 一覧表示
│   │   ├── FishingRecordDetail.tsx  # 詳細表示
│   │   ├── TideGraph.tsx            # 潮汐グラフ
│   │   ├── TideIntegration.tsx      # 潮汐統合コンポーネント
│   │   ├── PhotoUpload.tsx          # 写真アップロード
│   │   ├── FishSpeciesAutocomplete.tsx  # 魚種検索
│   │   ├── StatisticsDashboard.tsx  # 統計ダッシュボード
│   │   └── ...
│   │
│   ├── lib/                # サービス層（18個）
│   │   ├── database.ts                 # IndexedDB管理
│   │   ├── fishing-record-service.ts   # 釣果記録CRUD
│   │   ├── photo-service.ts            # 写真管理
│   │   ├── photo-metadata-service.ts   # EXIF抽出
│   │   ├── export-import-service.ts    # データエクスポート/インポート
│   │   ├── weather-service.ts          # 天気情報
│   │   ├── geolocation-service.ts      # GPS位置情報
│   │   ├── statistics-service.ts       # 統計計算
│   │   ├── settings-service.ts         # 設定管理
│   │   ├── image-processing.ts         # 画像処理
│   │   ├── image-optimizer.ts          # 画像最適化
│   │   ├── validation.ts               # バリデーション
│   │   ├── error-handler.ts            # エラーハンドリング
│   │   ├── retry-service.ts            # リトライ処理
│   │   ├── feedback-service.ts         # フィードバック管理
│   │   ├── user-guidance-service.ts    # ユーザーガイダンス
│   │   ├── analytics-service.ts        # アナリティクス
│   │   └── performance-monitor.ts      # パフォーマンス監視
│   │
│   ├── services/           # ドメインサービス
│   │   ├── tide/          # 潮汐計算システム（8個）
│   │   │   ├── TideCalculationService.ts    # 潮汐計算メインサービス
│   │   │   ├── CelestialCalculator.ts       # 天体力学計算
│   │   │   ├── HarmonicAnalysisEngine.ts    # 調和解析エンジン
│   │   │   ├── RegionalCorrectionEngine.ts  # 地域補正エンジン
│   │   │   ├── RegionalDataService.ts       # 地域データ管理
│   │   │   ├── TideClassificationEngine.ts  # 潮汐分類エンジン
│   │   │   ├── TideLRUCache.ts              # LRUキャッシュ
│   │   │   └── constants/                   # 定数定義
│   │   │
│   │   └── fish-species/  # 魚種検索システム（3個）
│   │       ├── FishSpeciesSearchEngine.ts   # 高速検索エンジン
│   │       ├── FishSpeciesDataService.ts    # データ管理サービス
│   │       └── FishSpeciesValidator.ts      # バリデーション
│   │
│   ├── stores/            # 状態管理（2個）
│   │   ├── app-store.ts   # グローバル状態（Zustand）
│   │   └── form-store.ts  # フォーム状態
│   │
│   ├── hooks/             # カスタムフック（4個）
│   │   ├── useAdvancedSearch.ts    # 高度な検索
│   │   ├── useFormValidation.ts    # フォームバリデーション
│   │   ├── usePWA.ts               # PWA機能
│   │   └── useResizeObserver.ts    # リサイズ監視
│   │
│   ├── types/             # 型定義（13個）
│   │   ├── entities.ts       # エンティティ型
│   │   ├── database.ts       # データベース型
│   │   ├── tide.ts           # 潮汐型
│   │   ├── fish-species.ts   # 魚種型
│   │   ├── forms.ts          # フォーム型
│   │   ├── state.ts          # 状態管理型
│   │   ├── media.ts          # メディア型
│   │   ├── location.ts       # 位置情報型
│   │   ├── metadata.ts       # メタデータ型
│   │   ├── export.ts         # エクスポート型
│   │   ├── analysis.ts       # 分析型
│   │   └── ...
│   │
│   ├── utils/             # ユーティリティ
│   │   ├── analysis/      # データ分析
│   │   ├── responsive/    # レスポンシブ対応
│   │   ├── scale/         # スケール計算
│   │   └── validation/    # バリデーション
│   │
│   ├── data/              # マスターデータ
│   │   ├── fish-species.json        # 魚種マスター（231種）
│   │   └── regional-tide-data.ts    # 地域別潮汐データ
│   │
│   ├── App.tsx            # メインアプリ（現在21,638行）
│   ├── ModernApp.tsx      # モダンアプリ（現在65,988行）
│   └── main.tsx           # エントリーポイント
│
├── docs/                  # ドキュメント
│   ├── ARCHITECTURE.md           # このファイル
│   ├── REFACTORING-PLAN.md      # リファクタリング計画
│   ├── design/
│   │   ├── fishing-record/
│   │   │   └── integrated-master-spec.md  # 釣果記録仕様
│   │   └── tide-system-master-spec.md     # 潮汐システム仕様
│   ├── technical-specifications.md   # 技術仕様
│   └── current-status-and-roadmap.md # ステータスとロードマップ
│
├── public/                # 静的ファイル
│   └── manifest.json      # PWAマニフェスト
│
└── tests/                 # テスト
    └── e2e/              # E2Eテスト（14個）
```

---

## 🔄 データフロー

### 釣果記録の作成フロー

```
User Action (フォーム入力)
      ↓
Component (FishingRecordForm.tsx)
      ↓
Store (form-store.ts) ← バリデーション
      ↓
Service (fishing-record-service.ts) ← ビジネスロジック
      ↓
Database (lib/database.ts) ← IndexedDB永続化
      ↓
Success Response
      ↓
UI Update (再レンダリング)
```

### 潮汐情報の取得フロー

```
User Location + Date/Time
      ↓
Component (TideIntegration.tsx)
      ↓
Service (TideCalculationService.ts)
      ↓
├─ Cache Check (TideLRUCache.ts) ← キャッシュヒット → Return
│       ↓ キャッシュミス
├─ Celestial Calculation (CelestialCalculator.ts)
├─ Harmonic Analysis (HarmonicAnalysisEngine.ts)
├─ Regional Correction (RegionalCorrectionEngine.ts)
└─ Classification (TideClassificationEngine.ts)
      ↓
Cache Store (TideLRUCache.ts)
      ↓
Return Tide Data
      ↓
Component Rendering (TideGraph.tsx)
```

### 写真アップロードフロー

```
User Action (写真選択)
      ↓
Component (PhotoUpload.tsx)
      ↓
Service (photo-metadata-service.ts) ← EXIF抽出
      ↓
Service (image-optimizer.ts) ← 画像最適化
      ↓
Service (photo-service.ts)
      ↓
Database (photos テーブル) ← Blob保存
      ↓
Auto-fill Form Fields (日時・位置情報)
```

---

## 🗄️ データベース設計

### IndexedDB構成

**データベース名**: `FishingRecordDB`
**バージョン**: 3

#### テーブル構成

1. **fishing_records** - 釣果記録（メインデータ）
2. **photos** - 写真データ（Blob）
3. **app_settings** - アプリ設定
4. **app_metadata** - アプリメタデータ
5. **tide_cache** - 潮汐キャッシュ
6. **tide_regional_data** - 潮汐地域データ
7. **geocode_cache** - ジオコーディングキャッシュ

詳細は `docs/DATABASE.md` を参照。

---

## 🎨 UI/UXパターン

### デザインシステム

- **カラーパレット**: Tailwind CSS デフォルトカラー
- **タイポグラフィ**: システムフォント（SF Pro, Roboto）
- **アイコン**: カスタムSVGアイコン
- **レスポンシブ**: モバイルファースト設計

### コンポーネント階層

```
AppLayout
├── ModernHeader (ヘッダー)
├── BottomNavigation (下部ナビ)
└── Main Content
    ├── HomePage (ホーム画面)
    │   ├── RecentRecordsSection
    │   ├── SpeciesChartSection
    │   └── TideStatisticsSection
    ├── FishingRecordList (一覧画面)
    ├── FishingRecordDetail (詳細画面)
    └── FishingRecordForm (フォーム画面)
```

---

## 🚀 ビルド・デプロイ

### ビルドプロセス

```bash
# 開発サーバー
npm run dev

# 本番ビルド
npm run build

# プレビュー
npm run preview
```

### デプロイ先

- **Vercel** - 推奨（自動デプロイ）
- **Netlify** - 代替

---

## 🔒 セキュリティ

### データプライバシー

- **ローカルストレージ**: 全データがIndexedDBに保存
- **外部通信なし**: API通信ゼロ
- **写真データ**: 端末内のみ（Blob保存）

### 認証

現在は**認証なし**（個人用途）

---

## 📈 パフォーマンス

### 最適化戦略

- **Code Splitting**: Viteの自動分割
- **Lazy Loading**: React.lazy()による遅延読み込み
- **Image Optimization**: 自動リサイズ・圧縮
- **LRU Cache**: 潮汐計算結果のキャッシュ
- **Virtual Scrolling**: 大量データの仮想化

### パフォーマンス目標

- **初回ロード**: < 3秒
- **インタラクション**: < 100ms
- **潮汐計算**: < 200ms（初回）、< 10ms（キャッシュ）

---

## 🧪 テスト戦略

### テストピラミッド

```
        /\
       /E2E\          ← Playwright（14個）
      /------\
     /  統合  \        ← Vitest（一部）
    /----------\
   / ユニット  \      ← Vitest（56個）
  /--------------\
```

### テスト種別

- **ユニットテスト**: サービス層・ユーティリティ
- **統合テスト**: コンポーネント統合
- **E2Eテスト**: ユーザーフロー全体

---

## 🔄 状態管理

### Zustand設計

#### app-store.ts（グローバル状態）

```typescript
interface AppState {
  // 釣果記録
  records: FishingRecord[];
  selectedRecord: FishingRecord | null;

  // UI状態
  isLoading: boolean;
  error: string | null;

  // 設定
  settings: AppSettings;

  // アクション
  fetchRecords: () => Promise<void>;
  createRecord: (data: CreateFishingRecordForm) => Promise<void>;
  updateRecord: (id: string, data: UpdateFishingRecordForm) => Promise<void>;
  deleteRecord: (id: string) => Promise<void>;
}
```

#### form-store.ts（フォーム状態）

```typescript
interface FormState {
  // フォームデータ
  formData: Partial<CreateFishingRecordForm>;

  // バリデーション
  errors: Record<string, string>;

  // アクション
  setFormData: (data: Partial<CreateFishingRecordForm>) => void;
  validate: () => boolean;
  reset: () => void;
}
```

---

## 📱 PWA対応

### Service Worker

- **キャッシュ戦略**: Cache First（静的ファイル）
- **オフライン対応**: IndexedDBで完全動作
- **インストール可能**: ホーム画面に追加

### manifest.json

```json
{
  "name": "Bite Note",
  "short_name": "Bite Note",
  "description": "釣果記録PWAアプリ",
  "start_url": "/",
  "display": "standalone",
  "theme_color": "#3b82f6",
  "background_color": "#ffffff"
}
```

---

## 🛠️ 開発ガイドライン

### コーディング規約

- **命名**: camelCase（変数・関数）、PascalCase（コンポーネント・型）
- **ファイル名**: kebab-case.tsx
- **コンポーネント**: 関数コンポーネント + Hooks
- **型定義**: 明示的な型付け必須

### Git規約

- **ブランチ**: `main`, `feature/*`, `refactor/*`
- **コミットメッセージ**: Conventional Commits
  - `feat:` 新機能
  - `fix:` バグ修正
  - `docs:` ドキュメント
  - `refactor:` リファクタリング
  - `chore:` その他

---

## 🔮 将来の拡張性

### フェーズ2以降の検討事項

- [ ] ページコンポーネント分割（ModernApp.tsx → pages/）
- [ ] マルチデバイス同期（ログイン機能追加時）
- [ ] プッシュ通知（潮汐アラート）
- [ ] ソーシャル機能（釣果共有）
- [ ] AI分析（釣果予測）

---

**最終更新**: 2025年10月30日
**作成者**: Bite Note Development Team
